// Replace the unknown types with the correct types for the following function

export const exercise1 = (
  age: number,
  fistName: string,
  lastName: string
): string => {
  return `Hello ${fistName} ${lastName}, you are ${age} years old`;
};
